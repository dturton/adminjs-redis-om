import { PropertyType } from 'adminjs';

const DATA_TYPES: Record<string, PropertyType> = {
  string: 'string',
  boolean: 'boolean',
  number: 'number',
  date: 'datetime',
};

export { DATA_TYPES };
